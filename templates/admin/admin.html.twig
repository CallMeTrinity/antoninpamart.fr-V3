{% extends 'base.html.twig' %}

{% block body %}
    <div class="min-h-screen py-6 flex flex-col justify-center sm:py-12">
        <div class="relative py-3 sm:max-w-xl sm:mx-auto">
            <div class="absolute inset-0 bg-gradient-to-r from-blue-300 to-blue-600 shadow-lg transform -skew-y-6 sm:skew-y-0 sm:-rotate-6 sm:rounded-3xl"></div>
            <div class="relative px-4 py-10 bg-white shadow-lg sm:rounded-3xl sm:p-20">
                <div class="max-w-md mx-auto">
                    <div>
                        <h1 class="text-2xl font-semibold">Page d'Administration</h1>
                    </div>
                    <div class="divide-y divide-gray-200">
                        <div class="py-8 text-base leading-6 space-y-4 text-gray-700 sm:text-lg sm:leading-7">
                            <ul class="list-disc space-y-2">
                                <li class="flex items-start">

                                    <p class="ml-2">
                                        <a href="{{ path('admin_projects') }}"
                                           class="text-lg text-blue-500 hover:text-blue-700">Gérer les Projets</a>
                                    </p>
                                </li>
                                <li class="flex items-start">

                                    <p class="ml-2">
                                        <a href="{{ path('admin_tags') }}"
                                           class="text-lg text-blue-500 hover:text-blue-700">Gérer les Tags</a>
                                    </p>
                                </li>
                                <li class="flex items-start">

                                    <p class="ml-2">
                                        <a href="{{ path('admin_skills') }}"
                                           class="text-lg text-blue-500 hover:text-blue-700">Gérer les Compétences</a>
                                    </p>
                                </li>
                                <li class="flex items-start">

                                    <p class="ml-2">
                                        <a href="{{ path('admin_me') }}"
                                           class="text-lg text-blue-500 hover:text-blue-700">Gérer mes infos</a>
                                    </p>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
