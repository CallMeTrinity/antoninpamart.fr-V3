{% extends 'base.html.twig' %}


{% block body %}
    <main class="text-white">
        <section class="pt-20 lg:pt-40 pb-40 relative">
            <div class="container mx-auto px-4">
                <div class="flex flex-wrap justify-center text-center">
                    <div class="w-full lg:w-6/12 px-4">
                        <h2 class="text-4xl font-semibold">Bienvenue sur mon Portfolio</h2>
                        <p class="text-lg leading-relaxed mt-4 mb-4 text-gray-500">Je suis Antonin, développeur
                            web.</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="relative py-20">
            <div class="container mx-auto px-4">
                <div class="flex flex-wrap">
                    <div class="w-full md:w-4/12 px-4 text-center">
                        <div class="text-gray-800 w-12 h-12 shadow-lg rounded-full inline-flex items-center justify-center">
                            <img src="{{ asset('media/logoProjets.svg') }}" alt="">
                        </div>
                        <h6 class="text-xl mt-5 font-semibold text-white"><a href="{{ path('project') }}">Projets</a></h6>
                        <p class="mt-2 mb-4 text-gray-500">Découvrez mes projets récents, reflétant mes compétences
                            et mon expertise.</p>
                    </div>

                    <div class="w-full md:w-4/12 px-4 text-center">
                        <div class="text-gray-800 w-12 h-12 shadow-lg bg-white rounded-full inline-flex items-center justify-center">
                            <img src="{{ asset('media/logoAbout.svg') }}" alt="">
                        </div>
                        <h6 class="text-xl mt-5 font-semibold text-white"><a href="{{ path('about') }}">À Propos</a></h6>
                        <p class="mt-2 mb-4 text-gray-500">En savoir plus sur moi, mon parcours et ce qui me motive
                            dans le développement web.</p>
                    </div>

                    <div class="w-full md:w-4/12 px-4 text-center">
                        <turbo-frame id="contact_frame">
                        <div class="text-gray-800 w-12 h-12 shadow-lg bg-white rounded-full inline-flex items-center justify-center">
                            <img src="{{ asset('media/logoContact.svg') }}" alt="">
                        </div>
                        <h6 class="text-xl mt-5 font-semibold text-white"><a href="{{ path('contact') }}">Contact</a></h6>
                        <p class="mt-2 mb-4 text-gray-500">N'hésitez pas à me contacter pour discuter de vos projets
                            ou pour toute question.</p>
                        </turbo-frame>
                    </div>
                </div>
            </div>
        </section>
    </main>
{% endblock body %}