<div{{ attributes }}>
    <div class="container mx-auto px-4 py-12">
        <div class="flex flex-wrap -m-4">
            {% for project in this.findProjects %}
                <twig:ProjectCard project="{{ project }}"/>
            {% endfor %}
        </div>
    </div>
</div>
